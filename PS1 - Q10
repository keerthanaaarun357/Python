n = str(input('Enter a number: '))

count = 0
        
for i in range(1, len(n)+1):
    
    if int(n) % int(n[-i]) == 0:
        print(n[-i], end = ' ')

    else:
        count += 1

if count == len(n):
    print('No factors')
